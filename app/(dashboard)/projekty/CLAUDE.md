# Modul Projekty — stránky

Pravidla modulu: viz `components/projekty/CLAUDE.md`. Pro stránky zejména: RSC pro čtení dat, mutace přes `app/api/projekty/**`, UI komponenty výhradně z `@/components/ui/*` (sdílená sada appky; modul nemá vlastní kopie), standardní design tokeny (nikdy `--notion-*`).
